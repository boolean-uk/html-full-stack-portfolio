{
	"name": "ex3",
	"version": "1.0.0",
	"description": "Today we are building a portfolio site for ourselves.  These types of sites use links to help visitors navigate the site. Your final site should look similar in layout to below:",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"watch-tailwindcss": "npx tailwindcss -i ./tailwind.css -o ./css.css --watch"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"tailwindcss": "^3.4.1"
	}
}
